build:
  docker:
    server: Dockerfile
    client: Dockerfile
  # config:
  #   BUNDLE_INSTALL_ARGS: --jobs 10 --retry=3 --without development test
run:
  server: npm run dev
  client: nom start
  # worker:
  #   command:
  #     - 
  #   image: server